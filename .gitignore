# --- Raw data & rendered outputs: large, regenerable, NEVER commit ---
*.npy
*.png
*.gif
*.mp4
# Keep the tiny condition sidecars as the experiment record:
!results/**/*.json

# --- Machine-specific configs (contain this box's serial by-id ports) ---
*.yaml
!*.example.yaml

# --- Python / uv ---
__pycache__/
*.pyc
.venv/
.uv/
# NOTE: commit uv.lock (reproducible installs) -- do not ignore it.

# --- OS noise ---
.DS_Store